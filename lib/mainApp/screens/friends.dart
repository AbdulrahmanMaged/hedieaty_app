import 'package:flutter/material.dart';

import '../widgets/add_friends.dart';

class AddFriends extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: AddFriendsWidget(),
    );
  }
}
